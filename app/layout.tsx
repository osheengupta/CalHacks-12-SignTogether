import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'SignTogether - Inclusive Video Meetings',
  description: 'Real-time, accessible video-meeting platform with AI-powered sign language detection and live captions',
  keywords: ['accessibility', 'sign language', 'video meetings', 'AI', 'inclusive'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-background">
          {children}
        </div>
      </body>
    </html>
  )
}
